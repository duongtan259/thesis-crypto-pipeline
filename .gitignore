# Secrets — never commit
.env
*.env

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
*.egg-info/
dist/
build/

# Measurement outputs. The JSON reports are cited by Chapter 5 of the thesis and are
# deliberately tracked; the raw JSON-lines captures are ~10 MB each and are the *input*
# to those reports, so they are excluded. Regenerate with scripts/capture_latency.py.
scripts/results/*.jsonl

# macOS
.DS_Store

# IDE
.vscode/
.idea/
*.swp

# Docker volumes
docker/volumes/
.worktrees/

# Working docs not part of the deliverable
docs/thesis_plan.md
docs/implementation_plan.md
docs/test_results.md
docs/pipeline_explained.md

# Superseded planning documents kept locally
docs/archive/
